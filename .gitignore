__pycache__/
*.pyc
.venv/
venv/
# downloaded test datasets — never push; the code prints fetch instructions
data/
*.csv
*.data
*.npz
# benchmark raw results — regenerable; only assets/*.png plots are committed
bench/results/
# packaging / test artifacts
build/
dist/
*.egg-info/
.pytest_cache/
# local tooling state
.claude/
